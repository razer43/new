# Prisma (SQLite)
#
# Option A (recommended for this repo): use the existing SQLite DB committed in /db/custom.db
DATABASE_URL="file:../db/custom.db"
#
# Option B: use a fresh local db under /prisma (will create prisma/dev.db)
# DATABASE_URL="file:./dev.db"


